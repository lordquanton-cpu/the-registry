# Push to GitHub

The repo is ready at `D:\Workspace\the-registry`. Initial commit is done.

---

## If the repo doesn't exist yet

1. Go to https://github.com/new
2. **Repository name:** `the-registry`
3. **Owner:** `lordquanton-cpu` (or your org/username)
4. **Visibility:** Public
5. Don't add README, .gitignore, or license (we have them)
6. Click **Create repository**

---

## Push

```bash
cd D:\Workspace\the-registry
git push -u origin main
```

If you use a different default branch (e.g. `master`), run:

```bash
git push -u origin main
# or
git push -u origin master
```

---

**Remote:** `https://github.com/lordquanton-cpu/the-registry.git`
